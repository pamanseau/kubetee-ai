apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "nemo-kubernetes-operator.fullname" . }}-nemoentityvalidator-editor-role
  labels:
  {{- include "nemo-kubernetes-operator.labels" . | nindent 4 }}
rules:
- apiGroups:
  - nvidia.com
  resources:
  - nemoentityvalidators
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - nvidia.com
  resources:
  - nemoentityvalidators/status
  verbs:
  - get