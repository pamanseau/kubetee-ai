---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.3
  name: runaidgxcstorages.runai.dgxc.nvidia.com
spec:
  group: runai.dgxc.nvidia.com
  names:
    kind: RunaiDGXCStorage
    listKind: RunaiDGXCStorageList
    plural: runaidgxcstorages
    singular: runaidgxcstorage
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Storage classes
      jsonPath: .status.storageClassesDisplayString
      name: StorageClasses
      type: string
    - description: Percent of storage quota used for volumes
      jsonPath: .status.storageUsedPct
      name: QuotaUsedPct
      type: string
    - description: Storage quota
      jsonPath: .status.storageQuota
      name: Quota
      type: string
    - description: Storage quota used by PVs
      jsonPath: .status.storageRequested
      name: QuotaUsed
      type: string
    - description: Storage utilized by PVs
      jsonPath: .status.storageConsumed
      name: StorageConsumed
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: RunaiDGXCStorage defines storage for DGXC Run:ai accelerated
          compute environments.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RunaiDGXCStorageSpec defines the desired storage state
            properties:
              instances:
                additionalProperties:
                  properties:
                    name:
                      description: Name of the PV
                      type: string
                    persistentVolumeReclaimPolicy:
                      description: PersistentVolumeReclaimPolicy describes a policy
                        for end-of-life maintenance of persistent volumes.
                      type: string
                  type: object
                type: object
              quota:
                type: string
              quotaID:
                type: string
                x-kubernetes-validations:
                - message: quotaID is immutable
                  rule: self == oldSelf
              tiers:
                items:
                  type: string
                type: array
            required:
            - quotaID
            type: object
          status:
            properties:
              altNames:
                description: Alternative names of the storage type
                items:
                  type: string
                type: array
              conditions:
                description: Conditions contains storage conditions
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              description:
                description: Short description for the quota
                type: string
              docs:
                description: Documentation for the storage class
                type: string
              instances:
                additionalProperties:
                  properties:
                    capacity:
                      description: Capacity of the PV
                      type: string
                    creationTime:
                      description: When the volume was created
                      format: date-time
                      type: string
                    fileShares:
                      additionalProperties:
                        properties:
                          name:
                            description: Name of the fileshare
                            type: string
                          origin:
                            description: |-
                              Origin what cluster was this Fileshare provisioned for
                              only matters in cases where multiple clusters are in the same CSP account
                            type: string
                          orphaned:
                            description: Orphaned is true if the volume is not mounted
                              in any cluster
                            type: boolean
                          values:
                            additionalProperties:
                              type: string
                            description: Usage metrics
                            type: object
                        required:
                        - name
                        type: object
                      description: Fileshare used for the volume
                      type: object
                    location:
                      description: Region where the resource is provisioned
                      type: string
                    name:
                      description: Name of the PV
                      type: string
                    persistentVolumeBoundStatus:
                      type: string
                    persistentVolumeReclaimPolicy:
                      description: PersistentVolumeReclaimPolicy describes a policy
                        for end-of-life maintenance of persistent volumes.
                      type: string
                    state:
                      description: Current state of the resource
                      type: string
                    tier:
                      description: Storage tier
                      type: string
                  type: object
                description: PVs consuming the quota
                type: object
              storageClasses:
                description: Storage classes that are bound to this quota
                items:
                  type: string
                type: array
              storageClassesDisplayString:
                description: StorageClassesDisplayString has a display string for
                  printing the storage classes from kubectl
                type: string
              storageConsumed:
                description: Amount of storage consumed from the requested
                type: string
              storageQuota:
                description: Amount of quota allocated
                type: string
              storageRequested:
                description: Amount of storage requested from the quota
                type: string
              storageUsedPct:
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
