apiVersion: v1
kind: Service
metadata:
  name: {{ include "nemo-common.servicename" . }}
  labels:
    {{- include "entity-store.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "entity-store.selectorLabels" . | nindent 4 }}
