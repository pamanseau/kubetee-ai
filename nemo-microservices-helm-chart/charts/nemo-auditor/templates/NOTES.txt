Installed {{ .Chart.Name }}-{{ .Chart.Version }}, named {{ .Release.Name }}.

The NeMo Auditor API is now available at:
{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
  {{- range .paths }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
  {{- end }}
{{- end }}
{{- else if .Values.virtualService.enabled }}
  https://{{ .Values.virtualService.dnsName }}{{ .Values.virtualService.apiPath }}
{{- else }}
  The service is running but no ingress or virtual service is configured.
  You can access it via port-forwarding:
  kubectl port-forward svc/{{ include "nemo-auditor.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}
{{- end }}

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get {{ .Release.Name }}
  $ helm test {{ .Release.Name }}
