apiVersion: v1
kind: Service
metadata:
  name: {{ include "nemo-common.servicename" . }}
  labels:
    {{- include "nemo-auditor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nemo-auditor.selectorLabels" . | nindent 4 }}
